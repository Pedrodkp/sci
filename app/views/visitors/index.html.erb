<%= render 'layouts/header_sci' %>

<!-- Page Content -->
<div class="container">

    <% if not user_signed_in? %>
        <p>Você não deveria estar aqui sem nós sabermos quem é, desculpe.</p>
        <p>Faça login no sistema e seja bem vindo!</p>              
    <!-- /.row -->
    <% else %>    
        <hr/>
        <center>
        <p>
            <%= form_tag(articles_path, :method => :get) do %>
              <%= link_to "Criar Artigo +".html_safe, new_article_path, :class => 'btn btn-default btn-lg' %>  
              <%= text_field_tag 'search_by_text', nil, class: "btn btn-default btn-lg", placeholder: 'Palavras chaves' %>
              <%= submit_tag "Buscar &raquo;".html_safe, class: "btn btn-default btn-lg" %>
            <% end %>        
            <hr/>
            <p>Um sistema desenhado para ajudar em suas dúvidas.</p>
            <p>Faça perguntas, começe discussões no fórum, e crie artigos que ajudaram outros!</p>                    
        </p>
        <hr/>
        <div class="row">
            <div class="col-sm-3">
                <h2><%= @articles.count %></h2>artigos no sistema.
            </div>
            <div class="col-sm-3">
                <h2><%= @articles.where(user_id: current_user.id).count %></h2>artigos que criei.
            </div>
            <div class="col-sm-3">
                <h2><%= @article_likes.where(user_id: current_user.id).count %></h2>artigos que me ajudaram.
            </div>
            <div class="col-sm-3">
                <h2><%= @posts.count %></h2>tópicos de fórum.
            </div>            
        </div>
        <hr/>
        <div class="panel panel-danger">
          <div class="panel-heading">
            Sistema em operação assistida em desenvolvimento, estamos liberando as funcionalidades em etapas.
          </div>
          <div class="panel-body">
            Se você tiver qualquer problema, dúvida ou sugestão por favor envie um e-mail para:<br/>
            <strong><%= mail_to("grupo.b@ecomex.com.br", name = nil, html_options = {}) %></strong>
          </div>
        </div>        
        </center>
    <% end %>
    <!-- /.row -->

    <!-- Footer -->
    <footer>
        <div class="row">
            <div class="col-lg-12">
                <p>Copyright &copy; NSI / By Soft 2016</p>
            </div>
        </div>
        <!-- /.row -->
    </footer>

</div>
<!-- /.container -->
